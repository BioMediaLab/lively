### This file was autogenerated by Nexus 0.11.3
### Do not make changes to this file directly


"""represents one of lively's classes"""
type Class {
  description: String!
  files(max: Int): [ClassFile!]!

  """Class ID"""
  id: String!
  members(max: Int): [ClassMember!]!
  name: String!
}

type ClassFile {
  class: Class!
  creator: User!
  description: String!
  file_key: String!
  file_name: String!
  id: ID!
  mimetype: String!
  url: String!
}

type ClassMember {
  class: Class!
  id: String!
  role: ClassRole!
  user: User!
}

"""A user's role in a given class"""
enum ClassRole {
  ADMIN
  ASSISTANT
  AUDITOR
  PROFESSOR
  STUDENT
}

input FileUpload {
  file: Upload
  name: String
}

type Mutation {
  cloneClassFile(new_class_id: ID, old_file: ID): ClassFile!
  createQuiz(class_id: ID, title: String): Quiz!
  deleteClassFile(file_id: ID!): ClassFile!
  loginGoogle(code: String, sessionInfo: String): Session!
  logout: Session!
  updateClassFile(description: String, file_id: ID, name: String): ClassFile!
  updateProfilePic(pic: FileUpload!): User!
  uploadClassFile(class_id: ID, description: String, file: FileUpload!): ClassFile!
}

type Query {
  class(class_id: ID): Class!
  classQuizzes(class_id: ID): [Quiz!]!
  googleRedirect: String!
  myClasses(
    """
    Return only classes where the user has a given role, such as "Admin"
    """
    role: ClassRole
  ): [ClassMember!]!
  myClassRole(class_id: ID): ClassMember!
  users: [User!]!
}

type Quiz {
  class_id: ID!
  id: ID!
  title: String!
}

type Session {
  """current user's id"""
  id: ID!

  """current user's session token"""
  session: String

  """
  A string describing the context (browser, ip) of the session. So the user can verify it.
  """
  sessionInfo: String
}

"""The `Upload` scalar type represents a file upload."""
scalar Upload

type User {
  email: String!
  id: ID!
  name: String!
}

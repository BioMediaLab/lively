### This file was autogenerated by Nexus 0.11.3
### Do not make changes to this file directly


"""represents one of lively's courses"""
type Course {
  description: String!

  """Course ID"""
  id: String!
  members(max: Int): [CourseUser!]!
  name: String!
}

"""A user's role in a given course"""
enum CourseRole {
  ADMIN
  ASSISTANT
  AUDITOR
  PROFESSOR
  STUDENT
}

type CourseUser {
  course: Course!
  id: String!
  role: CourseRole!
  user: User!
}

type Mutation {
  loginGoogle(code: String, sessionInfo: String): Session!
  logout: Session!
}

type Query {
  googleRedirect: String!
  myCourses: [CourseUser!]!
  users: [User!]!
}

type Session {
  """current user's id"""
  id: ID!

  """current user's session token"""
  session: String
  sessionInfo: String
}

type User {
  email: String!
  id: ID!
  name: String!
}
